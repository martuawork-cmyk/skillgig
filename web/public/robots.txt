# SkillGig.id — robots.txt
# Allow all crawlers by default; keep admin and API routes private.

User-agent: *
Allow: /
Disallow: /admin
Disallow: /admin/*
Disallow: /api
Disallow: /api/*

# Point crawlers at the sitemap.
Sitemap: https://skillgig.id/sitemap.xml